<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="test.com.moim.userinfo">
 
  <select id="U_SELECT_ALL" resultType="test.com.moim.userinfo.model.UserinfoVO">
	select * from userinfo order by num desc
  </select>
  
  <select id="U_SELECT_ONE" resultType="test.com.moim.userinfo.model.UserinfoVO">
  	select * from userinfo where num = #{num, jdbcType=VARCHAR }
  </select>
  
  <insert id="U_INSERT" parameterType="test.com.moim.userinfo.model.UserinfoVO">
  	insert into userinfo(num,user_id,pw,name,email,save_name) 
		values(seq_member.nextval,#{user_id, jdbcType=VARCHAR},#{pw, jdbcType=VARCHAR},#{name, jdbcType=VARCHAR},#{email, jdbcType=VARCHAR},#{save_name, jdbcType=VARCHAR})
  </insert>
  
  <select id="LOGIN" resultType="test.com.moim.userinfo.model.UserinfoVO">
		select * from userinfo where user_id = #{user_id, jdbcType=VARCHAR} and pw = #{pw, jdbcType=VARCHAR}
	</select>
  
  </mapper>
  